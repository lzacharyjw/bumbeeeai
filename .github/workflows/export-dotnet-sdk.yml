name: Export .NET 8 Build Parts

on:
  push:
  workflow_dispatch:

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - name: Install isolated .NET 8 SDK
        shell: bash
        run: |
          set -euxo pipefail
          mkdir -p "$RUNNER_TEMP/dotnet"
          curl -fsSL https://dot.net/v1/dotnet-install.sh -o "$RUNNER_TEMP/dotnet-install.sh"
          bash "$RUNNER_TEMP/dotnet-install.sh" --version 8.0.424 --install-dir "$RUNNER_TEMP/dotnet" --no-path
          "$RUNNER_TEMP/dotnet/dotnet" --info

      - name: Restore WPF build dependencies
        shell: bash
        run: |
          set -euxo pipefail
          export DOTNET_ROOT="$RUNNER_TEMP/dotnet"
          export PATH="$DOTNET_ROOT:$PATH"
          export NUGET_PACKAGES="$RUNNER_TEMP/nuget-packages"
          mkdir -p "$RUNNER_TEMP/restore-probe"
          cat > "$RUNNER_TEMP/restore-probe/RestoreProbe.csproj" <<'EOF'
          <Project Sdk="Microsoft.NET.Sdk">
            <PropertyGroup>
              <OutputType>WinExe</OutputType>
              <TargetFramework>net8.0-windows</TargetFramework>
              <UseWPF>true</UseWPF>
              <UseWindowsForms>true</UseWindowsForms>
              <EnableWindowsTargeting>true</EnableWindowsTargeting>
              <RuntimeIdentifiers>win-x64;win-x86</RuntimeIdentifiers>
              <SelfContained>true</SelfContained>
            </PropertyGroup>
            <ItemGroup>
              <PackageReference Include="Microsoft.Web.WebView2" Version="1.0.2792.45" />
            </ItemGroup>
          </Project>
          EOF
          dotnet restore "$RUNNER_TEMP/restore-probe/RestoreProbe.csproj" --packages "$NUGET_PACKAGES"

      - name: Download approved background and package parts
        shell: bash
        run: |
          set -euxo pipefail
          curl --fail --location --retry 4 'https://developersaccess.com/uploads/domains/samZCvmTwyAQZVcZksJfmdtnKXp0rkfF.webp' --output "$RUNNER_TEMP/approved-weedleaf.webp"
          test -s "$RUNNER_TEMP/approved-weedleaf.webp"
          sha256sum "$RUNNER_TEMP/approved-weedleaf.webp"
          tar -C "$RUNNER_TEMP" -czf dotnet8-sdk.tar.gz dotnet
          tar -C "$RUNNER_TEMP" -czf dotnet8-wpf-nuget.tar.gz nuget-packages
          cp "$RUNNER_TEMP/approved-weedleaf.webp" approved-weedleaf.webp
          ls -lh dotnet8-sdk.tar.gz dotnet8-wpf-nuget.tar.gz approved-weedleaf.webp

      - name: Upload .NET SDK
        uses: actions/upload-artifact@v4
        with:
          name: dotnet8-sdk
          path: dotnet8-sdk.tar.gz
          compression-level: 0
          retention-days: 1

      - name: Upload WPF NuGet cache
        uses: actions/upload-artifact@v4
        with:
          name: dotnet8-wpf-nuget
          path: dotnet8-wpf-nuget.tar.gz
          compression-level: 0
          retention-days: 1

      - name: Upload approved background
        uses: actions/upload-artifact@v4
        with:
          name: approved-weedleaf
          path: approved-weedleaf.webp
          compression-level: 0
          retention-days: 1
